// The six equipment slots. Order here doesn't affect the UI layout --
// that's controlled by where each EquipmentSlotUI node is placed in the
// equipment grid in the editor.
public enum EquipSlot
{
	Head,
	Body,
	Legs,
	Feet,
	LeftHand,
	RightHand
}
